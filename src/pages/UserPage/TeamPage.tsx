import type { FC } from 'react';
import { Typography } from 'antd';

export const TeamPage: FC = () => {

  return (
    <>
        <Typography.Title level={3}>Team Page</Typography.Title>
    </>
  );
};